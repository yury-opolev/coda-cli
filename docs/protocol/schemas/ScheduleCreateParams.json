{
  "type": "object",
  "properties": {
    "name": {
      "type": [
        "string",
        "null"
      ]
    },
    "prompt": {
      "type": "string"
    },
    "every": {
      "type": [
        "string",
        "null"
      ],
      "description": "Exactly one of `every`, `at` or `cron` must be set."
    },
    "at": {
      "type": [
        "string",
        "null"
      ]
    },
    "cron": {
      "type": [
        "string",
        "null"
      ]
    },
    "timeZone": {
      "type": [
        "string",
        "null"
      ]
    },
    "maxRuns": {
      "type": [
        "integer",
        "null"
      ],
      "format": "uint32",
      "minimum": 1,
      "description": "Stop after this many runs have been *started*. A run that later fails\nstill consumes one. Requires the `schedules.bounds` capability: an\nengine without it accepts the field and ignores it."
    },
    "expiresAt": {
      "type": [
        "string",
        "null"
      ],
      "description": "Absolute ISO-8601 deadline; no run starts at or after it. Mutually\nexclusive with `expires_in`."
    },
    "expiresIn": {
      "type": [
        "string",
        "null"
      ],
      "description": "Relative deadline (`\"30m\"`, `\"2h\"`, `\"7d\"`), resolved to an absolute\ninstant once, at creation. Mutually exclusive with `expires_at`."
    }
  },
  "required": [
    "prompt"
  ],
  "title": "ScheduleCreateParams",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
